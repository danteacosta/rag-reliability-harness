# Routing

FastAPI maps URL paths to Python functions using route decorators.

## Path parameters

Path parameters are declared in the route path using curly braces.

## Query parameters

Query parameters are optional key-value pairs appended after `?` in the URL.

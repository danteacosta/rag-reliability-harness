# Contributing

Install with `python -m pip install -e '.[dev]'` and run `python -m pytest -q`.
Keep fixtures synthetic, avoid credentials/customer data, and update contract
tests and documentation when report schemas change.
